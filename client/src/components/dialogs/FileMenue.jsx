import { Menu } from "@mui/material";
import React from "react";

const FileMenue = ({ anchorEl }) => {
  return (
    <Menu open={false} anchorEl={anchorEl}>
      sds
    </Menu>
  );
};

export default FileMenue;
